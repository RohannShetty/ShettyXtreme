# Section 01 — Reverse-Engineering Lens

For each of the 8 references: **best used as** / **inherit** / **NOT copy** / **coupling risk** / **external vs internal** / **upstream harvest**. Built from the Phase-0 briefs ([Section 00 — Research Method](00-research-method.md)); sections 09-15 carry the detail. License gate (pack §33-38): AGPL = ideas only, MIT/Apache = safe to adapt, no-license = read/link only.

## 2.1 FinceptTerminal — breadth reference (AGPL-3.0 + commercial) · `docs/references/BRIEF-fincept.md`

| Best used as | Inherit | NOT copy | Coupling risk | External vs internal | Upstream harvest |
|---|---|---|---|---|---|
| Product-breadth catalog + options-analytics concept checklist (C++20/Qt6, 54 screens) | Screen taxonomy (dashboard widgets, watchlist, action center); options: chain, multi-leg builder + leg model, payoff + net greeks, OI/max-pain, IV surface (OTM convention), GEX, FII/DII; math: Black-76 on forward, lot-size scaling; canonical instrument model + broker adapters; HITL gate + kill-switch | Any code/structure/UX (AGPL + USD 50k commercial license); C++/Qt stack; SaaS QuantLab; global breadth; their OpenAlgo-derived options scripts | Zero: concepts only; never open a Fincept source while implementing a mirrored feature (`BRIEF-fincept.md` §5) | External, read-only; not vendored/imported; the brief is the only artifact | None for code — ever; catalog static (maintenance throttled) |

## 2.2 OpenAlgo — vendoring reference (AGPL-3.0) · `docs/references/BRIEF-openalgo-upstream.md`

| Best used as | Inherit | NOT copy | Coupling risk | External vs internal | Upstream harvest |
|---|---|---|---|---|---|
| Execution-plumbing adaptation source; the only reference from which **code** is taken (per D1). Vendored set: 10 A-tier + 6 B-tier Phase-2 files — exact list in [Section 10 — OpenAlgo Utilization](10-openalgo-utilization.md) | A-tier files as **vendored adaptation source** (never imported; adaptations implement `core/interfaces` protocols); SL-M → protective STOP_LOSS with tick snapping + MPP slabs (2.0.1.6); plugin.json discovery + lazy auth-import | Server layer (`app.py`, blueprints, frontend, restx_api, auth_db, sandbox, websocket server, order services, misc app modules); never vendor from contaminated `D:\OpenAlgo` (v2.0.1.4) — mirror v2.0.1.7 only | The one real surface: origin stamps (AGPL + s5a modification notice), monthly `scripts/sync_vendor.py` sync, `FILES.yaml` manifest, src never imports vendor | External upstream (mirror `references/upstream/openalgo`, gitignored) → internal tracked `vendor/openalgo/` | Monthly diff-review per release notes; watch `broker/dhan/*`, `mpp_slab.py`, `transform_data.py`, `order_adapter.py` |

## 2.3 ShettyBot V1 — prior-version DNA (ours) · pack §24-30 + `docs/references/STATUS.md`

| Best used as | Inherit | NOT copy | Coupling risk | External vs internal | Upstream harvest |
|---|---|---|---|---|---|
| The intelligence/learning DNA of v2 (v0.6.0, ~495 tests, Phase 3 delivered; archived per D5); module seams listed in [Section 02 — Current-State Reaudit](02-current-state-reaudit.md), per-module disposition in [Section 09 — ShettyBot Evolution](09-shettybot-evolution.md) — the surviving DNA: event bus + Protocols, deterministic voters/regime, risk + cost model, 6-tracker learning loop | Superseded stances (pack §33-38): 806-as-credential-mixing, feed codes 2/8, loose `dhanhq>=0.1.0`, env-dependent mode default; landmines (dead imports in `hints/`/`conviction/`, VoterRegistry stub, stale conftest fixtures, empty dirs) are Phase-2 fixes | Low — our own code; archived, not installed: v2 never imports the v1 tree; transfer via blueprint + Phase-2 migration | Internal (our history); `references/upstream/shettybot-v1` is the authoritative status copy | None recurring — one-time per-module migration audit in Phase 2 |

## 2.4 DhanHQ-py — SDK dependency (MIT) · `docs/references/BRIEF-dhanhq-upstream.md`

| Best used as | Inherit | NOT copy | Coupling risk | External vs internal | Upstream harvest |
|---|---|---|---|---|---|
| The broker SDK: one `DhanContext(client_id, access_token)` serves trading REST + api-feed.dhan.co WS + historical (D8 single-primary model); pinned 2.2.0, mirror `D:\DhanHQ-py-2.2.0` | Use as-is via `DhanContext` — order/position/trade-book/fund calls, EDIS/TPIN, position conversion; `MarketFeed` v2 (codes 15/17/21, disconnect RequestCode 12, error codes 805-809); historical + option-chain endpoints; consent + PIN/TOTP token flows — full surface mapping in [Section 11 — Dhan Integration](11-dhan-integration.md) | Nothing to copy — it is a dependency; its gaps are ours: no auto token refresh (~3AM IST), no REST retry, 1s blind WS reconnect, no rate-limit/staleness handling; don't adopt 2.3.0rc1 additions yet | Version-gated: pin `>=2.2.0,<2.3.0` (a yanked 2.1.0 exists); watch 5 contract files: `marketfeed.py`, disconnect/806-807 semantics, `DhanHTTP` envelope, `auth.py`, historical/option-chain | External PyPI dependency; our `integration/dhan/` owns the SDK's gaps: 806 → stop loop (entitlement), 807 → renewal, backoff+jitter, 30s staleness watchdog, self-throttling | Version-gated: diff the 5 contract files on each 2.3.x stable; 2.3.0rc1 byte-identical on auth/feed/historical/option-chain |

## 2.5 Quant-Developers-Resources — pointer index (no license) · `docs/references/BRIEF-quant-developers-resources.md`

| Best used as | Inherit | NOT copy | Coupling risk | External vs internal | Upstream harvest |
|---|---|---|---|---|---|
| Curriculum + coverage checklist, mapped in [Section 13 — Systematic Trading Breadth](13-systematic-trading-breadth.md); competitive intelligence (Indian quant firms); no code exists in it | Top-10 items: Option Chain Analyser concept (ATM/ITM/OTM classification, live chain, payoff, IV surface — swap KiteConnect for Dhan); reading canon (Natenberg, Sinclair, Hull, Taleb, de Prado walkforward = our learning-loop methodology); Risk Management curriculum (VaR/CVaR/ES, drawdown, greeks, GARCH, stress); Python-for-finance checklist (Black-Scholes, greeks, VaR + Kupiec); Econometrics outline; Technical_Indicators catalog | Content wholesale (no license); US career/interview material; HFT systems dirs (C++, FPGA, CUDA, HPC, quantum); direct PDF links to copyrighted books — buy them; empty/stale dirs | None — no code, no dependency, no vendoring; knowledge comes from the original books/papers, acquired legitimately | External, read-only; only citations and feature-map entries enter the repo | Occasional re-check; links rot (lnkd.in), treat as a static index |

## 2.6 ai-hedge-fund — agentic research patterns (MIT) · `docs/references/BRIEF-ai-hedge-fund.md`

| Best used as | Inherit | NOT copy | Coupling risk | External vs internal | Upstream harvest |
|---|---|---|---|---|---|
| Architecture reference for the Phase-3 research workspace: analysts produce views, deterministic code disposes, same pipeline backtests and runs live | Blackboard state handoff (`AgentState` + `analyst_signals[agent_id]`); config-registry agent discovery; Pydantic structured outputs (json-mode, 3x retry, safe default); deterministic risk funnel + PM bounded to pre-validated actions; same-code-path backtesting (v2 `run_cycle`); point-in-time discipline. v2's principle: "The LLM never touches the trade." | US data-provider coupling (`financialdatasets.ai`); any code path generating trades from agent votes (SimBroker is paper-only); celebrity personas as edge (keep the styles as lenses); US risk/portfolio math (252-day annualization, short+margin); the FastAPI+SQLite app layer | None — patterns only; no imports, no shared schema, no license contamination (MIT). Live signals stay deterministic ([Section 14 — Data-Decision Intelligence](14-data-decision-intelligence.md), [Section 12 — AI & Agentic References](12-ai-agentic-references.md)) | External, pattern-only; the brief's §5 mapping table is the transfer document | Low; v2 principles captured; re-read only for new state/contract patterns |

## 2.7 anthropics/financial-services — governance patterns (Apache-2.0) · `docs/references/BRIEF-anthropics-financial-services.md`

| Best used as | Inherit | NOT copy | Coupling risk | External vs internal | Upstream harvest |
|---|---|---|---|---|---|
| Governance reference for the Phase-3 AI research layer: propose-never-bind, tiered tool isolation, schema-validated outputs, research → gate → critic → approve → execute loop ([Section 12 — AI & Agentic References](12-ai-agentic-references.md)) | "Propose, never bind"; tiered isolation (reader: untrusted docs, validated JSON out; orchestrator: read-only data MCPs; write-holder: sole Write/Edit); critic subagent; deny-by-default tools; `output_schema` validation (additionalProperties: false, caps, enums); handoff allowlist; kyc-rules grid (rule_id/outcome/evidence) as risk-engine filter chain; stop-and-surface checkpoints; intent payload sketch | US regulatory framing (FINRA/SEC, sanctions/PEP, KYC/AML); banking/fund-admin flows; compliance ceremony; marketplace machinery; Claude-specific plumbing (agent.yaml, `agent_toolset_20260401`, Cowork manifests) | High only if Claude-specific plumbing is adopted — we don't; patterns port to any LLM provider; gate/allowlist/kill-switch are provider-agnostic; rewrite-adapted, not vendored | External pattern library; the brief's §2.5 mapping table is the transfer document | Low; repo warns its deploy loop is reference-only; capture new guardrails on occasional re-reads |

## 2.8 awesome-design-md — design format & style source (MIT) · `docs/references/BRIEF-awesome-design-md.md`

| Best used as | Inherit | NOT copy | Coupling risk | External vs internal | Upstream harvest |
|---|---|---|---|---|---|
| Format specification (9-section Stitch contract) + style reference for a dark, data-dense trading workstation ([Section 15 — Design System & Terminal UX](15-design-system-terminal-ux.md)) | Frontmatter-generation format (YAML tokens + `{token.refs}`); Binance: trading semantics, numeric typography first-class (tabular face), dark canvas + surface ladder; VoltAgent: dark-only chrome, hairline elevation, single accent, mono metrics; ClickHouse: near-pure-black canvas + 3-step surface ladder. Then **invert** red/green to the Indian convention (red = rise, green = fall) | Marketing light themes (Ollama, Stripe, Coinbase); playful palettes (PostHog, Sentry); scale that fights density; Binance's yellow voltage + gradient hero; all decorative systems; referenced brands' colors/typefaces | None — design tokens are ours, derived from picks; no code, no assets, MIT-safe | External, inspiration-only; the brief's §5 token list is the transfer list | None recurring; format spec stable; re-read only if Stitch's spec changes materially |

## 3. Synthesis

Licenses per pack §33-38 (AGPL ×2, MIT ×3, Apache ×1, none ×1, ours ×1). Recurring sync: OpenAlgo monthly (D1), DhanHQ version-gated (5 contract files), ShettyBot one-time; the other five are static inputs whose durable artifact is the brief. Update discipline: [Section 07 — Update-Resilient Design](07-update-resilient-design.md); boundaries: [Section 05 — System Boundaries](05-system-boundaries.md), [Section 18 — Repo & Codebase Strategy](18-repo-codebase-strategy.md).
