# Phase 7 UI Polish — R1: Dialog enter/exit animation (120ms)

**Date:** 2026-08-06
**Status:** COMPLETE
**Mission source:** `docs/superpowers/plans/2026-08-05-phase7-ui-review.md` R1 (line 165, 183-213)
**Constraint honored:** DESIGN.md §4 — transitions capped at 120ms; subtle enter/exit, no motion dep.

## 0. Conclusion (read this first)

Dialogs and the command palette no longer snap — every `DialogPrimitive` overlay/content now fades in/out with a subtle zoom over exactly 120ms. Implemented via the `tw-animate-css` dependency (Tailwind v4 `@utility` set), wired into the existing `DialogContent` primitive so **all** dialogs (ModeSwitcher, KillSwitch, ProposalQueue confirm, ShortcutsDialog, CommandPalette) inherit it for free. Verified: `svelte-check` 0 errors, production build succeeds, and the compiled CSS bundle contains the generated `fade-in-0` / `zoom-in-95` / `fade-out-0` / `zoom-out-95` utilities.

## 1. Changes

| File | Change |
|---|---|
| `web/package.json` | Added `tw-animate-css` ^1.4.0 (dependencies). `package-lock.json` regenerated by `npm i` (1 package, 0 vulnerabilities) |
| `web/src/lib/app.css` | Added `@import "tw-animate-css";` after the `tailwindcss` import |
| `web/src/lib/components/ui/dialog/dialog-content.svelte` | Overlay: `duration-[120ms] data-[state=open]:animate-in data-[state=open]:fade-in-0 data-[state=closed]:animate-out data-[state=closed]:fade-out-0`. Content: same plus `data-[state=open]:zoom-in-95` / `data-[state=closed]:zoom-out-95` |
| `web/src/terminal/static/` (built bundle) | Regenerated by `npm run build` (committed-bundle convention) |

Exact review-doc reference (review.md:183-201) matched verbatim; no deviation.

## 2. Verification

- `npm run check` → **svelte-check found 0 errors and 0 warnings**
- `npm run build` → success, `../static/` emitted (`index-02mLgK2c.css` 96.74 kB, `index-CzZHoFGV.js` 454.60 kB), built in 1m 13s
- Compiled bundle contains the animation utilities: `fade-in-0` ✓, `zoom-in-95` ✓, `fade-out-0` ✓, `zoom-out-95` ✓, plus the `--tw-enter-opacity` / `--tw-exit-scale` custom properties — proof the `tw-animate-css` import and `data-[state]` classes compiled through Tailwind v4
- No Python/test gates affected (frontend-only change; no `.venv` suite run required)

## 3. Notes

- `tw-animate-css@1.4.0` ships `dist/tw-animate.css` with `animate-in/out` bases and wildcard `@utility fade-in-*` / `zoom-in-*` / `zoom-out-*` rules (confirmed in the package before wiring); Tailwind v4 resolves the numeric suffixes at build time.
- Animation keys off bits-ui's existing `data-state` attributes — no JS, no event-handler changes, no behavior change.
- Reduced-motion: R1 was scoped to the 120ms fade/zoom only; the `prefers-reduced-motion` guard for the kill-pulse (separate R-item) is out of scope.
- Working tree carries parallel-lane changes (risk/settings backend, other Phase-7 lanes) — not touched here; diff restricted to the three web files above plus the regenerated bundle.

## 4. Out of scope (per mission)

No commit. No files outside the three listed targets (plus the required bundle output and the report).
